太好了，你这个问题已经完全站在“自己实现 sidecar”这条正路上了。
我不帮你写代码，而是明确告诉你：每一层该选哪些 Go 包、每一层你的实现目标是什么、做到什么程度就算“成功”。
只要你照着这个 checklist 做，你一定能做出来，而且不会走偏。

总体思路（非常重要，先记住）

从“完全不解析协议” → “只感知连接/流” → “保持 gRPC stream 透明”

三层不是并列的，是递进关系。

第一层：TCP Transparent Sidecar（地基）
🎯 目标（必须达成）

不关心协议

不拆包、不重组

原样转发字节流

对 HTTP / HTTP2 / gRPC 全部有效

👉 这是 sidecar 的生存能力

📦 选包（只用标准库）
net
io
context


你 不需要：

net/http

gin

grpc

🧠 你实现时要关注的点（这很重要）
1️⃣ 双向 copy

client → upstream

upstream → client

任一方向 EOF 即关闭连接

2️⃣ 连接生命周期

不泄露 goroutine

处理半关闭（FIN）

3️⃣ 性能

不要包太多 buffer

io.Copy 足够

✅ 完成标准（自检）

curl / grpcurl 都能直接穿透

HTTP/2 多路复用正常

gRPC streaming 不被打断

第二层：HTTP/2 Aware Proxy（开始“懂协议”）
🎯 目标（不是做 Envoy）

区分 HTTP/1.1 与 HTTP/2，并在 HTTP/2 层级“感知 stream”

你不是要重写 HTTP/2，只是不破坏它

📦 选包（关键）
net/http
golang.org/x/net/http2


不要用：

gin

fasthttp

🧠 实现关注点（非常关键）
1️⃣ 强制 HTTP/2

TLS 自动启用

或支持 h2c

2️⃣ 使用 http2.Transport

让 Go 帮你 multiplex stream

你不要手动管理 stream ID

3️⃣ Handler 是 “代理”，不是“业务”

你的 Handler 要做到：

不解析 body

不修改 header（除非学习用）

请求 → upstream

响应 → 原样写回

✅ 完成标准（自检）

浏览器 / curl --http2 正常

多并发请求复用一个 TCP 连接

gRPC 请求不会被当成普通 HTTP 解析失败

第三层：gRPC Stream Passthrough（最容易走偏）
🎯 目标（非常明确）

保持 gRPC 的 stream 语义完整

你做的是 “passthrough”，不是 “terminate”

📦 选包（两种路线）
路线 A（推荐，最稳）
net


👉 gRPC 走 TCP passthrough
👉 HTTP/2 multiplex 由底层负责

这是 Istio sidecar 最早期方式

路线 B（进阶，难）
google.golang.org/grpc


使用：

grpc.UnknownServiceHandler

但注意：

你会接管 stream lifecycle

错误率高

不适合第一版

🧠 你实现时“千万别做的事”

❌ 读完整 message 再转发

❌ 改 Content-Type

❌ 新建连接 per RPC

✅ 完成标准（自检）

Unary RPC 正常

Server Stream 不断流

Client Stream 不断流

BiDi Stream 不断流

三层合在一起后的正确结构（脑中模型）
Listener
 └── Connection
     ├── TCP passthrough        ← 所有流量
     └── (optional)
         └── HTTP/2 aware       ← 只“观察”
             └── gRPC passthrough


👉 注意：HTTP/2 / gRPC 都是 TCP 之上的优化层

常见误区（你已经避开一半了）
❌ 用 gin 做 sidecar

gin 是“应用框架”，不是“代理核心”

❌ 想一次把 HTTP + gRPC 全代理

顺序必须是：TCP → HTTP/2 → gRPC

❌ 想做“功能齐全版 Envoy”

你现在学的是原理，不是重写 Envoy

给你的学习 milestone（强烈建议）
Milestone 1（你现在）

TCP transparent sidecar

Milestone 2

HTTP/2 aware（只打印 metadata）

Milestone 3

gRPC stream passthrough（0 改动）

Milestone 4（可选）

filter（统计 / logging）

一句话总结（你可以写在项目 README）

This project is a learning-oriented sidecar proxy exploring TCP-level transparency, HTTP/2 multiplexing awareness, and gRPC stream passthrough in Go.

这已经是非常成熟的工程表述了。

如果你愿意，下一步我可以：

给你一份 sidecar 项目目录结构建议

或 对照 Envoy，帮你做一张“我这一步在干嘛”的对照表

你选一个，我继续带。